import LandingClient from './LandingClient';
export const metadata = { title: 'Vertex — Plataforma de Recompensas', description: 'Gana puntos, completa misiones y desbloquea recompensas.', robots: { index: false, follow: false } };
export default function HomePage() { return <LandingClient />; }
