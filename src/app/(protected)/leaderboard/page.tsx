import LeaderboardClient from './LeaderboardClient';
export const metadata = { title: 'Clasificación — Vertex', robots: { index: false, follow: false } };
export default function LeaderboardPage() { return <LeaderboardClient />; }
