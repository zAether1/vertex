import MissionsClient from './MissionsClient';
export const metadata = { title: 'Misiones — Vertex', robots: { index: false, follow: false } };
export default function MissionsPage() { return <MissionsClient />; }
