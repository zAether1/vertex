import RedemptionsClient from './RedemptionsClient';

export const metadata = {
  title: 'Mis Redenciones — Vertex',
  robots: { index: false, follow: false },
};

export default function RedemptionsPage() {
  return <RedemptionsClient />;
}
