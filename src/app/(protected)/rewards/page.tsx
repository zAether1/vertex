import RewardsClient from './RewardsClient';
export const metadata = { title: 'Recompensas — Vertex', robots: { index: false, follow: false } };
export default function RewardsPage() { return <RewardsClient />; }
