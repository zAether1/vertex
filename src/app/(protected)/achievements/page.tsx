import AchievementsClient from './AchievementsClient';
export const metadata = { title: 'Logros — Vertex', robots: { index: false, follow: false } };
export default function AchievementsPage() { return <AchievementsClient />; }
