import ArcadeClient from './ArcadeClient';
export const metadata = { title: 'Arcade — Vertex', robots: { index: false, follow: false } };
export default function ArcadePage() { return <ArcadeClient />; }
