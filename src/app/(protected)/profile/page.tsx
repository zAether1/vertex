import ProfileClient from './ProfileClient';
export const metadata = { title: 'Perfil — Vertex', robots: { index: false, follow: false } };
export default function ProfilePage() { return <ProfileClient />; }
