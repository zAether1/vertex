import RegisterClient from './RegisterClient';
export const metadata = { title: 'Crear cuenta — Vertex', robots: { index: false, follow: false } };
export default function RegisterPage() { return <RegisterClient />; }
